import styled from '@emotion/styled';

import { ColumnPageContainer } from '@components/Layout/Layout';

export const CalendarSectionContainer = styled(ColumnPageContainer)({
    alignItems: 'flex-start',
    flex: 'unset',
    width: '350px',
});
