import { Coordinates } from '@typings/atbat';

interface StrikeZoneProps {
    coordinates: Coordinates[];
}

const StrikeZone = ({ coordinates }: StrikeZoneProps) => {
    return <div>StrikeZone</div>;
};

export default StrikeZone;
